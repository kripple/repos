// import { api } from '@/api/createApi';

// export const useProfileApi = () => {};

// export const useApi = (option: 'profile' | 'repo' | 'repos') => {
//   const timestamp = Date.now().toString();
//   const response = api.useGetRateLimitQuery(timestamp); // do not cache
// };
